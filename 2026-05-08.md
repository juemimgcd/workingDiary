# 2026-05-08 工作日志

## 今日目标

5月8日主要处理 AtlasClaw 项目中 `D:\python_files\bugs_fund\2026_05_08.md` 记录的前两个问题，并把修复提交到独立分支，准备通过 fork 向公司仓库发起 Pull Request。

本次工作覆盖：

1. 修复 `user_id` 直接作为运行时路径片段时可能逃出 `workspace/users` 边界的问题。
2. 修复 `session_key` 放在 URL path 参数中，字段包含 `/` 或 `\` 时 session 历史、重置、删除、状态等接口无法访问的问题。
3. 整理当前分支、commit、fork remote 和 PR 发起方式。

## Bug 1：user_id 直接进入运行时路径导致用户目录边界风险

### 问题现象

多个运行时模块原来直接把外部传入的 `user_id` 拼进路径，例如：

```text
<workspace>/users/<user_id>/sessions
<workspace>/users/<user_id>/memory
<workspace>/users/<user_id>/work_dir
```

如果 `user_id` 来自外部身份源，并包含路径分隔符或上级目录片段，例如：

```text
..\..\outside_workspace
```

就可能让 session、memory、workspace、heartbeat、hook、user_setting 等运行时文件写到预期用户目录之外，破坏 per-user 存储隔离。

### 修复方式

新增统一的用户运行时路径工具：

```text
app/atlasclaw/core/user_paths.py
```

其中 `normalize_runtime_user_id()` 会把外部 `user_id` 转成稳定的单一路径片段，`user_runtime_dir()` 统一返回：

```text
<workspace>/users/<safe_user_id>
```

本次把相关运行时路径改为复用这个工具，避免各模块各自拼接原始 `user_id`。涉及的模块包括 session、memory、workspace、security guard、API user setting、provider bindings、runtime hooks 和 heartbeat store 等。

同时，`MemoryManager` 的默认路径也调整到文档和 hook sink 使用的结构：

```text
<workspace>/users/<user_id>/memory
```

并补充了 legacy `memory/<user_id>` 到新位置的迁移测试。

### 修改文件

主要涉及：

- `app/atlasclaw/core/user_paths.py`
- `app/atlasclaw/session/manager.py`
- `app/atlasclaw/memory/manager.py`
- `app/atlasclaw/core/workspace.py`
- `app/atlasclaw/core/security_guard.py`
- `app/atlasclaw/api/api_routes.py`
- `app/atlasclaw/core/user_provider_bindings.py`
- `app/atlasclaw/hooks/runtime_store.py`
- `app/atlasclaw/hooks/runtime_sinks.py`
- `app/atlasclaw/heartbeat/store.py`
- `tests/atlasclaw/test_workspace.py`
- `tests/atlasclaw/memory/test_memory_isolation.py`

### 测试覆盖

新增或调整的重点测试包括：

```text
test_user_id_path_segments_are_encoded
test_legacy_user_subdir_migrates_to_documented_location
```

这些测试用于验证外部 `user_id` 不会逃出 `workspace/users`，以及旧版 memory 用户目录可以迁移到文档要求的新路径。

## Bug 2：session_key 包含 URL 路径分隔符时 session path API 访问失败

### 问题现象

`session_key` 会由外部字段组成，例如 `peer_id`、`channel`、`thread_id`。当这些字段里包含 `/` 或 `\` 时，即使前端用 `encodeURIComponent(sessionKey)` 构造 URL，FastAPI/Starlette 在路由匹配阶段仍可能把 `%2F` 当作路径分隔语义处理，导致类似接口返回 404：

```text
GET /api/sessions/{session_key}/history
POST /api/sessions/{session_key}/reset
DELETE /api/sessions/{session_key}
GET /api/sessions/{session_key}/status
```

复现例子：

```json
{"scope": "per-peer", "peer_id": "team/42"}
```

对应的 session key 中会带有来自 `peer_id` 的路径分隔语义，导致历史、状态、重置、删除等 path API 不能稳定访问。

### 修复方式

在 `app/atlasclaw/session/context.py` 中扩展 session key segment 的编码逻辑。之前已经处理了 `:` 和 `%`，本次继续把 URL/path 相关分隔符编码进去：

```text
/  -> %2F
\  -> %5C
```

反序列化时再恢复原始字段，保证 session key 字符串可放进 URL path，同时不丢失原始 `peer_id`、`channel`、`thread_id`。

在 `app/atlasclaw/api/routes_session.py` 中，把 session 操作接口改为使用 `{session_key:path}` 捕获，并在进入业务逻辑前通过 `_canonical_session_key()` 还原成规范的 encoded session key。这样既兼容已有 URL 访问方式，也能把路由捕获到的 key 统一回内部 canonical 形式。

### 修改文件

主要涉及：

- `app/atlasclaw/session/context.py`
- `app/atlasclaw/api/routes_session.py`
- `tests/atlasclaw/session/test_session_key.py`
- `tests/atlasclaw/test_session_api_routes.py`

### 测试覆盖

新增或调整的重点测试包括：

```text
test_roundtrip_escapes_url_path_separators_in_segments
test_direct_session_routes_accept_keys_with_path_separators
```

覆盖内容包括：

- session key 序列化结果中不再出现原始 `/` 或 `\`。
- 反序列化后仍能恢复原始 `channel`、`peer_id`、`thread_id`。
- `get session`、`history`、`reset`、`status`、`queue`、`compact`、`delete` 等直接 session 路由可以接受带路径分隔来源字段的 session key。

## 本地验证

检查当前分支和提交状态：

```powershell
git status --short --branch
git log -5 --date=iso --pretty="format:%h %ad %d %s"
git show --stat --format=fuller f93a175
```

当前分支：

```text
fix/runtime-user-path-session-url
```

当前提交：

```text
f93a175 fix(session): harden runtime user paths and session key routing
```

提交统计：

```text
20 files changed, 234 insertions(+), 60 deletions(-)
```

尝试运行针对 `2026_05_08.md` 的复现测试和本次改动相关回归测试：

```powershell
pytest ../bugs_fund/pytest_2026_05_08.py -q -p no:cacheprovider --tb=short
python -m pytest ../bugs_fund/pytest_2026_05_08.py -q -p no:cacheprovider --tb=short
py -m pytest ../bugs_fund/pytest_2026_05_08.py -q -p no:cacheprovider --tb=short
```

以及：

```powershell
pytest tests/atlasclaw/session/test_session_key.py tests/atlasclaw/test_session_api_routes.py tests/atlasclaw/test_workspace.py tests/atlasclaw/memory/test_memory_isolation.py -q -p no:cacheprovider --tb=short
python -m pytest tests/atlasclaw/session/test_session_key.py tests/atlasclaw/test_session_api_routes.py tests/atlasclaw/test_workspace.py tests/atlasclaw/memory/test_memory_isolation.py -q -p no:cacheprovider --tb=short
py -m pytest tests/atlasclaw/session/test_session_key.py tests/atlasclaw/test_session_api_routes.py tests/atlasclaw/test_workspace.py tests/atlasclaw/memory/test_memory_isolation.py -q -p no:cacheprovider --tb=short
```

当前环境未能实际执行测试，原因是：

```text
pytest: 无法将“pytest”项识别为 cmdlet、函数、脚本文件或可运行程序的名称
python.exe: 系统找不到指定的路径
py: No installed Python found
```

因此本次只能确认已补充测试代码和本地 Git 证据，不能记录为测试已通过。

## 分支和提交

本次修复分支：

```text
fix/runtime-user-path-session-url
```

本地和 fork 远端分支状态：

```text
fix/runtime-user-path-session-url f93a175 [fork/fix/runtime-user-path-session-url]
```

提交信息：

```text
fix(session): harden runtime user paths and session key routing
```

remote 状态：

```text
origin -> https://github.com/CloudChef/atlasclaw.git
fork   -> https://github.com/cc-JianqiZheng/atlasclaw.git
```

当前工作区还有一个未跟踪文件：

```text
summary.md
```

该文件没有进入本次 commit。

## PR 情况

由于项目是 fork 的公司项目，PR 应从个人 fork 分支提交到公司仓库主分支：

```text
cc-JianqiZheng/atlasclaw:fix/runtime-user-path-session-url
  -> CloudChef/atlasclaw:main
```

推荐 PR 标题：

```text
fix(session): harden runtime user paths and session key routing
```

可直接打开的 compare 页面：

```text
https://github.com/CloudChef/atlasclaw/compare/main...cc-JianqiZheng:fix%2Fruntime-user-path-session-url?expand=1
```

本地未安装 `gh` CLI，因此没有通过命令行创建 PR；需要通过 GitHub 网页完成创建。

## 总结

5月8日完成了 `2026_05_08.md` 中前两个 bug 的代码修复整理和分支提交。第一个问题通过统一 `user_id` 运行时路径规范化，避免外部用户标识直接影响 `workspace/users` 下的目录结构；第二个问题通过扩展 session key segment 编码和调整 session path 路由捕获方式，避免 `/`、`\` 等 URL 路径分隔符导致 session 操作接口无法访问。

当前修复已经提交在 `fix/runtime-user-path-session-url`，并且本地可见该分支已关联到 `fork/fix/runtime-user-path-session-url`。剩余事项是通过 GitHub 网页从 fork 分支向 `CloudChef/atlasclaw:main` 创建 PR，并在有可用 Python/pytest 环境后补跑相关回归测试。

## 追加记录：Bug 3 / Bug 4 memory 修复

### 背景

在前两个 session/runtime 路径问题修复后，继续处理 `2026_05_08.md` 中剩余的两个 memory 相关问题：

1. 内置 `memory_search` / `memory_get` 工具对真实 `MemoryManager` 不可用。
2. `MemoryManager` 存储路径需要和架构文档、Hook `MemorySink` 的 `users/<user_id>/memory/` 目录保持一致。

本次工作仍在分支：

```text
fix/runtime-user-path-session-url
```

新增提交：

```text
de3e0b7 fix(memory): enable built-in tools for real manager
```

### Bug 3：内置 memory_search / memory_get 对真实 MemoryManager 不可用

#### 问题现象

工具层原来只从 `deps.extra["memory_manager"]` 取 manager，并且只在对象存在 `search()` / `get()` 方法时调用。真实运行时注入的是 `deps.memory_manager`，而真实 `MemoryManager` 原本没有这两个方法，因此会出现：

```text
memory_search -> (no matching memories)
memory_get    -> (memory_get not supported for path: ...)
```

这会让 agent 误以为 memory 工具已经正常执行，但实际没有读到真实长期记忆。

#### 修复方式

在 `app/atlasclaw/memory/manager.py` 中补充真实接口：

```text
MemoryManager.search(query, limit=10)
MemoryManager.get(path, offset=None, limit=None)
```

`search()` 会扫描当前用户目录下的 Markdown memory 文件：

```text
users/<user_id>/memory/*.md
```

覆盖 `MEMORY.md`、每日记忆文件，以及 hook / compaction 生成的 `memory_<timestamp>.md`。搜索结果会带上：

```text
path
start_line
end_line
```

供 `memory_search` 输出 citation。`get()` 会读取指定 memory 文件的行片段，并限制目标路径必须仍在当前用户的 memory 目录内，避免工具读取越界文件。

同时调整两个工具：

- `app/atlasclaw/tools/memory/search_tool.py`
- `app/atlasclaw/tools/memory/get_tool.py`

工具现在会优先兼容旧的 `deps.extra["memory_manager"]`，同时支持真实运行时的 `deps.memory_manager`。如果 manager 不支持对应接口，会返回 error，不再静默返回“不支持”的普通文本。

#### 测试覆盖

在 `tests/atlasclaw/test_memory_tool_citations.py` 中增加真实 `MemoryManager` 回归测试：

```text
test_memory_search_uses_real_memory_manager_from_deps
test_memory_get_uses_real_memory_manager_from_deps
```

这两项覆盖：

- `memory_search` 可以从真实 `MemoryManager.write_long_term()` 写入的 `MEMORY.md` 中搜索到内容。
- `memory_get` 可以通过 `deps.memory_manager` 读取真实 memory 文件片段。
- tool details 中包含稳定的 `path`、`start_line`、`end_line` 和 citation。

### Bug 4：MemoryManager 存储路径与文档和 Hook MemorySink 不一致

#### 问题现象

架构文档和 Hook `MemorySink` 使用的长期记忆目录是：

```text
<workspace>/users/<user_id>/memory/
```

旧问题中 `MemoryManager` 曾使用另一套目录：

```text
<workspace>/memory/<user_id>/
```

这会导致 Hook Runtime 或历史压缩写入的 memory 与 `MemoryManager` 读取的 memory 分裂成两套目录，互相不可见。

#### 修复状态

当前分支中 `MemoryManager` 已统一使用：

```text
user_runtime_dir(workspace, user_id) / "memory"
```

也就是文档要求的：

```text
users/<safe_user_id>/memory/
```

并保留旧目录迁移逻辑：

```text
memory/*.md                  -> users/default/memory/*.md
memory/<user_id>/*.md        -> users/<user_id>/memory/*.md
```

本次在修复 Bug 3 时，`MemoryManager.search()` 也按这个统一目录扫描所有用户 memory Markdown 文件，保证 Hook MemorySink、历史压缩和 MemoryManager 工具看到同一套 memory 数据。

另外，`read_long_term()` 的 Markdown 解析也做了修正：原逻辑会在 `MEMORY.md` 以一级标题开头时跳过整个文件；现在改为跳过标题行但保留正文，避免 manager 自己写入的长期记忆读不回来。

### 修改文件

本次 memory 修复提交包含：

```text
app/atlasclaw/memory/manager.py
app/atlasclaw/tools/memory/get_tool.py
app/atlasclaw/tools/memory/search_tool.py
tests/atlasclaw/test_memory_tool_citations.py
```

提交统计：

```text
4 files changed, 276 insertions(+), 11 deletions(-)
```

后续还整理了 `docs/MODULE-DETAILS.MD` 中 MemoryManager API 表格，使文档与真实接口一致；该文档改动在记录时仍处于本地未提交状态。

### 本地验证

这次环境里 `python` / `py` / `pytest` 不可直接执行，但 `uv` 可用。由于默认 uv cache 指向 `D:\Dev\uv\cache` 时权限不足，临时把 cache 目录切到仓库内后完成验证。

验证命令：

```powershell
$env:UV_CACHE_DIR='D:\python_files\atlasclaw\.uv-cache'; uv run pytest tests/atlasclaw/test_memory_tool_citations.py tests/atlasclaw/memory/test_memory_isolation.py ../bugs_fund/pytest_2026_05_08.py -q -p no:cacheprovider --tb=short
```

结果：

```text
15 passed in 3.51s
```

继续跑 memory / security 相关回归：

```powershell
$env:UV_CACHE_DIR='D:\python_files\atlasclaw\.uv-cache'; uv run pytest tests/atlasclaw/test_memory.py tests/atlasclaw/test_security_restrictions.py tests/atlasclaw/test_memory_tool_citations.py -q -p no:cacheprovider --tb=short
```

结果：

```text
22 passed in 0.36s
```

同时执行：

```powershell
git diff --check
```

没有发现 whitespace 错误，仅有 Windows 换行转换提示。

### PR / fork 状态

本次 memory 修复提交已在 fork 分支上：

```text
fork/fix/runtime-user-path-session-url -> de3e0b7
```

需要注意：因为复用了 `fix/runtime-user-path-session-url` 分支，GitHub 会把新提交追加到同一个公司 PR 中，而不是基于同一个 head 分支再创建第二个公司 PR。

曾误创建过一个 fork 内部 PR：

```text
CloudChef:main -> cc-JianqiZheng:main
```

这个 PR 只是把公司 main 合回个人 fork main，不是本次修复提交给公司的方向，可以关闭，不需要 merge。

正确的公司 PR 方向应是：

```text
cc-JianqiZheng:fix/runtime-user-path-session-url -> CloudChef:main
```

### 小结

5月8日后续补完了 memory 相关两个问题。真实 `MemoryManager` 现在具备内置 memory 工具需要的 `search()` / `get()` 接口，工具层也能读取运行时注入的 `deps.memory_manager`；memory 文件统一在 `users/<user_id>/memory/` 下读写和搜索，和架构文档、Hook MemorySink、历史压缩路径保持一致。相关仓库内回归测试和原始复现测试均已通过。




